<div class="page-header">
	<h1><?=__('Classif', 'control', 'pageTitle')?></h1>
</div>
<?php
$datagrid = new UI_Datagrid('coherence', 'datagrid_consistency', 'classif');

$columnControl = new UI_Datagrid_Col_Text('control', __('Classif', 'control', 'seekedIssue'));
$datagrid->addCol($columnControl);

$columnDiag = new UI_Datagrid_Col_Bool('diag', __('UI', 'name', 'diagnostic'));
$columnDiag->valueTrue = '<i class="icon-ok"></i> '.__('UI', 'other', 'ok');
$columnDiag->valueFalse = '<i class="icon-remove"></i> '.__('UI', 'other', 'nok');
$datagrid->addCol($columnDiag);

$columnFail = new UI_Datagrid_Col_Text('fail', __('Classif', 'control', 'abnormalItems'));
$datagrid->addCol($columnFail);


$datagrid->pagination = false;
$datagrid->display();
