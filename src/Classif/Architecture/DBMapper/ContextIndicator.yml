Classif\Domain\ContextIndicator:
  type: entity
  repositoryClass: \Classif\Architecture\Repository\ContextIndicatorRepository
  table: Classif_ContextIndicator

  id:
    context:
      associationKey: true
    indicator:
      associationKey: true

  manyToOne:
    context:
      targetEntity: Classif\Domain\Context
      joinColumn:
        name: idContext
        referencedColumnName: id
    indicator:
      targetEntity: Classif\Domain\Indicator
      joinColumn:
        name: idIndicator
        referencedColumnName: id

  manyToMany:
    axes:
      targetEntity: Classif\Domain\IndicatorAxis
      joinTable:
        name: Classif_ContextIndicator_Axes
        joinColumns:
          idContext:
            referencedColumnName: idContext
          idIndicator:
            referencedColumnName: idIndicator
        inverseJoinColumns:
          idAxis:
            referencedColumnName: id
